[tool.poetry]
name = "glitter_proto"
version = "0.1.0"
description = ""
authors = ["Glitter Protocol <ted@glitterprotocol.io>"]
readme = "README.md"
packages = [{include = "glitter_proto"}]

[tool.poetry.dependencies]
python = "^3.7"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
