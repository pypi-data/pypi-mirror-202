[build-system]
requires = [
    "setuptools>=61.0", 
]
build-backend = "setuptools.build_meta"

[project]
name    = "maxdmg_back"
version = "0.0.5"
authors = [
  { name="tonotonokon" },
]
dependencies = [
]
description = "This is a backend for maxdmg project."
readme = "README.md"
requires-python = ">=3.10"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
