[tool.poetry]
name = "init-tikz"
version = "0.1.15"
description = "Added linenos and fontsize."
authors = ["vaibhavblayer <vaibhavblayer@gmail.com>"]
readme = "README.md"
packages = [{include = "init_tikz"}]

[tool.poetry.scripts]
init-tikz = "init_tikz.main:main"

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
