[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "continue_sestinj"
version = "0.0.1"
authors = [
  { name="Nate Sesti", email="nate@continue.dev" },
]
description = ""
readme = "README.md"
requires-python = ">=3.7"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved",
]

[project.urls]
"Homepage" = "https://continue.dev"