[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "filedups"
version = "0.4.0"
authors = [
  { name="Armağan Salman", email="armagansalman+pypi@gmail.com" },
]
description = "Local file duplicate finder"
readme = "README.md"
requires-python = ">=3.7"
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]

[project.urls]
"Homepage" = "https://bitbucket.org/armaganymmt/filedups/src/main/"
