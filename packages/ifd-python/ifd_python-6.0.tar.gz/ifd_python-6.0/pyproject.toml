[tool.poetry]
name = "ifd-python"
version = "6.0"
description = ""
authors = ["Antonin Lemoine <antonin.lemoine@altitudeinfra.fr>"]
readme = "README.md"
packages = [{include = "ifd"}]

[tool.poetry.dependencies]
python = "^3.6"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
