[tool.poetry]
name = "database-io"
version = "0.1.0"
description = ""
authors = ["Dan <kelleherjdan@gmail.com>"]
readme = "README.md"
packages = [{include = "database_io"}]

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
