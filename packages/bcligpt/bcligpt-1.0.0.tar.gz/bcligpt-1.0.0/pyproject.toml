[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
[project]
name = "bcligpt"
version = "1.0.0"
description = "Better CLI for GPT-3.5"
dependencies = [
    "openai", "tiktoken", "rich"
]
readme = "README.md"
authors = [
    { name = "Marco Filippozzi", email = "marco.filippozzi98@gmail.com" }
    ]
[project.scripts]
bcligpt = "bcligpt:start"
