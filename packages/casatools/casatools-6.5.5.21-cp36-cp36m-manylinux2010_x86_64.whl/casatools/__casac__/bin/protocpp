#!/usr/bin/perl
###########################################################################
########################## generated by setup.py ##########################
###########################################################################
use File::Basename;
use Cwd 'abs_path';
use Cwd 'getcwd';

$plugindir = abs_path(dirname(abs_path($0)) . "/grpc_cpp");
$bindir    = abs_path(dirname(abs_path($0)));
$ENV{PATH} = "$plugindir:$ENV{PATH}";
exec("$bindir/protoc", @ARGV);
